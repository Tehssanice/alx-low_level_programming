#include "main.h"

/**
 * _abs - This program prints the absolute value of an integer.
 * @n: The integer to be computed.
 *
 * Return: int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
